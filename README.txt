config文件
配置登录用户名、密码，
update字段用来更新课程信息，如果是首次访问，则应该设置为True
之后由于课表信息固定，可以设置为False
time字段用来设置刷课请求的延迟，单位为秒
depids字段用来设置学院代码，即选修的课程在哪个学院，学院代码可以查看depid.json文件，
如果不确定学院代码，可以设置为 depids=[]

courseId文件
用来设置待选课的代码，后面的on表示设置为学位课